# Register Functions

This example shows how to register external functions to have scripts impact outside code.   There are 3 functions registers.  `getX` and `setX` show how you can modify variables outside of the script while `pow` shows an example of multiple arguements being passed to a function.